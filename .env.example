# Local deterministic mode; no external calls or credentials required.
AI_MODE=mock

# Enable only when AI_MODE=live. This remains server-side.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-5-mini

# Reserved for future provider selection.
SONG_RECOGNITION_PROVIDER=mock
LYRICS_PROVIDER=mock
MUSIC_ANALYSIS_PROVIDER=mock
